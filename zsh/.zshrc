. "$HOME/.zshrc.env"
. "$HOME/.zshrc.omz"
. "$HOME/.zshrc.options"

. "$HOME/.zshrc.vars"
. "$HOME/.zshrc.tools"
. "$HOME/.zshrc.apikeys"
. "$HOME/.zshrc.aliases"
. "$HOME/.zshrc.functions"

# bun completions
[ -s "/home/skimhi/.bun/_bun" ] && source "/home/skimhi/.bun/_bun"
