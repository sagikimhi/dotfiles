[ -e "$HOME/.zshrc.tools.uv"        ] && . "$HOME/.zshrc.tools.uv"
[ -e "$HOME/.zshrc.tools.go"        ] && . "$HOME/.zshrc.tools.go"
[ -e "$HOME/.zshrc.tools.bun"       ] && . "$HOME/.zshrc.tools.bun"
[ -e "$HOME/.zshrc.tools.tex"       ] && . "$HOME/.zshrc.tools.tex"
[ -e "$HOME/.zshrc.tools.fnm"       ] && . "$HOME/.zshrc.tools.fnm"
[ -e "$HOME/.zshrc.tools.dvt"       ] && . "$HOME/.zshrc.tools.dvt"
[ -e "$HOME/.zshrc.tools.perl"      ] && . "$HOME/.zshrc.tools.perl"
[ -e "$HOME/.zshrc.tools.mise"      ] && . "$HOME/.zshrc.tools.mise"
[ -e "$HOME/.zshrc.tools.java"      ] && . "$HOME/.zshrc.tools.java"
[ -e "$HOME/.zshrc.tools.mars"      ] && . "$HOME/.zshrc.tools.mars"
[ -e "$HOME/.zshrc.tools.julia"     ] && . "$HOME/.zshrc.tools.julia"
[ -e "$HOME/.zshrc.tools.pgsql"     ] && . "$HOME/.zshrc.tools.pgsql"
[ -e "$HOME/.zshrc.tools.cargo"     ] && . "$HOME/.zshrc.tools.cargo"
[ -e "$HOME/.zshrc.tools.pyapp"     ] && . "$HOME/.zshrc.tools.pyapp"
[ -e "$HOME/.zshrc.tools.gerrit"    ] && . "$HOME/.zshrc.tools.gerrit"
[ -e "$HOME/.zshrc.tools.zoxide"    ] && . "$HOME/.zshrc.tools.zoxide"
[ -e "$HOME/.zshrc.tools.apache"    ] && . "$HOME/.zshrc.tools.apache"
[ -e "$HOME/.zshrc.tools.duckdb"    ] && . "$HOME/.zshrc.tools.duckdb"
[ -e "$HOME/.zshrc.tools.svlint"    ] && . "$HOME/.zshrc.tools.svlint"
[ -e "$HOME/.zshrc.tools.sigasi"    ] && . "$HOME/.zshrc.tools.sigasi"
[ -e "$HOME/.zshrc.tools.surelog"   ] && . "$HOME/.zshrc.tools.surelog"
[ -e "$HOME/.zshrc.tools.starship" ] && . "$HOME/.zshrc.tools.starship"
[ -e "$HOME/.zshrc.tools.verible"   ] && . "$HOME/.zshrc.tools.verible"
[ -e "$HOME/.zshrc.tools.iverilog"  ] && . "$HOME/.zshrc.tools.iverilog"
[ -e "$HOME/.zshrc.tools.verilator" ] && . "$HOME/.zshrc.tools.verilator"
[ -e "$HOME/.zshrc.tools.opentitan" ] && . "$HOME/.zshrc.tools.opentitan"
[ -e "$HOME/.zshrc.tools.tailscale" ] && . "$HOME/.zshrc.tools.tailscale"
